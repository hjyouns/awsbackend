server:
  port: 8080

spring:
  application:
    name: aws-lab-backend
  datasource:
    url: {DB_URL:jdbc:mysql://database-1-instance-1.c3s26k4iyom0.ap-northeast-2.rds.amazonaws.com:3306/aws-lab?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: {DB_USERNAME:admin}
    password: {DB_PASSWORD:admin}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
